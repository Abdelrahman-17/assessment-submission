# Assessment Notes & Execution Summary

## 📌 Overview
This repository contains the completed technical assessment tasks, organized and structured as requested. Below is a brief summary of each component:

### 1. Task 1: Log Analyzer (`task-1-log-analyzer/`)
- **Script:** `analyzer.py` successfully parses raw logs and extracts key security events.
- **Outputs:** Generated `output/events.csv` and `output/risk_report.csv` containing categorized risk data.

### 2. Task 2: Flask Audit App (`task-2-flask-audit/`)
- **Application:** A functional Flask web application (`app.py`) with user interface templates and static assets.
- **Purpose:** Handles audit logging and tracking seamlessly.

### 3. Task 3: AI Prompt App (`task-3-ai-prompt-app/`)
- **Application:** Developed an interactive AI prompt interface (`app.py` with custom HTML templates).

### 4. Task 4: Ansible Basics Lab - Bonus (`task-4-ansible-bonus/`)
- **Automation:** Verified local configuration management using `inventory.ini` and `basic_setup.yml`.
- **Idempotency Confirmed:** Subsequent playbook runs successfully yield `changed=0`, validating stable infrastructure state.

---
*All tasks have been tested locally and verified for full functionality.*
